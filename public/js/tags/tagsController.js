/**
 * Created by Komo on 03/02/2015.
 */

/*
angular.module('komon.controllers').controller('tagsController', ['$scope', '$http', '$filter', '$interval', '$timeout', 'tagsService',
    function ($scope, $http, $filter, $interval, $timeout, tagsService) {

        console.log("ee");

        $scope.data.komonerTags = [
            { text: 'just' },
            { text: 'some' },
            { text: 'cool' },
            { text: 'tags' }
        ];

        $scope.data.loadKomonerTags = function (query) {
            tagsService.getKomonerTags($scope.komonerId, query).then(function(result)
            {
                console.log(result);
                $scope.komonerTags = result;
            });
        };

    }]);
*/
